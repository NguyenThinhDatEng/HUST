#include<iostream>
#include<iomanip>
#include<math.h>
using namespace std;

int main()
{
    const int divisor = pow(10, 9) + 7;
    unsigned long long a, b;
    cin >> a >> b;
    unsigned long long c = a % divisor;
    
    return 0;
}